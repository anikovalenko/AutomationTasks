package org.example.unit_testing;

public class Calculator {

    //ctrl + shift + T on method name to create a corresponding file system in Test package
    int sum(int num1, int num2) {
        return num1 + num2;
    }

    int divide(int num1, int num2) {
        return num1 / num2;
    }
}
